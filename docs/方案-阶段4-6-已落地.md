# 方案 · 阶段 4–6（资源库 · Pack · Token）— 已落地

> 状态：**已实现（精简版）**  
> 说明：按「提高效率、做完再验收」执行；比总路线更克制，保证可维护。

## 4 资源库（图标）

| 项 | 实现 |
|----|------|
| Material Symbols | 复用 `public/material-symbols.json`（约 3900）全量检索（≥2 字） |
| Lucide / Heroicons | **常用名对照表** → 画布仍用 Material 字形（`lib/icons.ts`） |
| UI | 颜色/主题面板下的 **图标库**：搜索、来源筛选、点选应用到当前部件 |
| 局限 | 未内置完整 Lucide SVG；对照映射够用，完整 SVG 为后续 |

## 5 设计系统 Pack（骨架）

| Pack | 规则 |
|------|------|
| **Material 3** | 默认；角色色板；rounded |
| **shadcn / Neutral** | mono 色板；square；system 字体；提示词改写 |

- 左侧「设计系统 Pack」一键切换色板+主题  
- AI 生成 prompt 注入 pack 提示  
- **完整 shadcn 组件绘制** 仍属后续大 Pack 工程  

## 6 Token / 导出

| 能力 | 实现 |
|------|------|
| 设计 Token | 一键导出 `prism-tokens-*.css`（CSS 变量） |
| 设计 JSON | 原有 saveProject / 项目库 |
| 提示词 | 原有 Prompt 面板 + Pack/设备增强 |

## 验收自检

- [x] typecheck / test / build  
- [x] 颜色面板可见 Pack 切换与 Token 导出  
- [x] 选中部件后颜色面板底部出现图标库  
- [ ] 人工：点图标改部件；切 shadcn 看变灰阶方角  

## 后续 backlog

- 完整 Lucide SVG 包  
- 更多 Pack（Ant / Element）  
- 组件级 shadcn 绘制（非仅 theme）  
- 模板库（原阶段 7）  
